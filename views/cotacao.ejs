<%- include('header')%>
    <blockquote class="blockquote text-center">
        <p class="mb-0">
            <% if(error){ %>
                <%= error%>
                    <%}else{%>
                        <%= quantidade %> dolares na cotação
                            <%= cotacao%> é igual a:
                                <%= conversao%> reais
                                    <%}%>
        </p>
    </blockquote>

    <%- include('footer')%>